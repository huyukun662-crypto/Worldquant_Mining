__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
cache/
MINING_REPORT.json
.optuna*
credential.txt
credentials.txt
*.brainrc
# In-flight scan/refine outputs are overwritten after each simulation.
# Once complete, individual runs may be committed (the previous miner
# committed WQ_MINING_REPORT.json and WQ_SUBMISSION_RESULTS.json the
# same way) but per-sim snapshots stay out of the index.
