[
  {
    "expr": "zscore(ts_decay_linear(add(group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(assets, 90))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(add(group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(assets, 90))), subindustry)), group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(equity, 90))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(group_neutralize(zscore(divide(ts_backfill(fnd6_acqgdwl, 90), ts_backfill(assets, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(fnd6_acqintan, 90), ts_backfill(assets, 90))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(group_neutralize(zscore(divide(ts_backfill(fnd6_pstkc, 90), ts_backfill(equity, 90))), subindustry), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(group_neutralize(zscore(ts_backfill(earnings_per_share_standard_deviation, 90)), subindustry), group_neutralize(zscore(ts_backfill(sales_estimate_standard_deviation, 90)), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(group_neutralize(zscore(ts_backfill(earnings_per_share_standard_deviation, 90)), subindustry), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(group_neutralize(zscore(divide(ts_backfill(fnd6_xacc, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(fnd6_xacc, 90), ts_backfill(assets, 90))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(group_neutralize(zscore(divide(ts_backfill(fnd6_acdo, 90), ts_backfill(assets, 90))), subindustry), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(group_neutralize(reverse(zscore(divide(ts_backfill(cashflow_dividends, 90), ts_backfill(cashflow, 90)))), subindustry), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(add(group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(assets, 90))), subindustry)), group_neutralize(zscore(divide(ts_backfill(fnd6_acqgdwl, 90), ts_backfill(assets, 90))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(reverse(zscore(divide(ts_backfill(cashflow_dividends, 90), ts_backfill(cashflow, 90)))), subindustry)), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  },
  {
    "expr": "zscore(ts_decay_linear(add(add(group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(fnd6_mfma2_revt, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(rd_expense, 90), ts_backfill(assets, 90))), subindustry)), add(group_neutralize(zscore(divide(ts_backfill(fnd6_drc, 90), ts_backfill(assets, 90))), subindustry), group_neutralize(zscore(divide(ts_backfill(fnd6_drlt, 90), ts_backfill(assets, 90))), subindustry))), 10))",
    "settings": {
      "universe": "TOP3000",
      "delay": 1,
      "decay": 6,
      "neutralization": "INDUSTRY",
      "truncation": 0.005
    }
  }
]